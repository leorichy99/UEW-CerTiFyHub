# AGENTS.md

Project: UEW CerTiFyHub — certificate generation, verification, and admin platform for the University of Education, Winneba.

Stack: Django REST backend + React (Vite, Konva-based template editor) frontend, co-located in this repo.

## Agent skills

### Issue tracker

Issues and PRDs live on GitHub at `leorichy99/UEW-CerTiFyHub`. Use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical names: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root covering both backend and frontend. See `docs/agents/domain.md`.
